Contains FIT CLI workflows, primarily for generating and running definition-driven flows.

Reusable elements that may be usable by other workflows should be placed under `src/shared`.
Reusable elements that may be usable by other pieces, not just workflows, should be placed under `src/steps` or `src/lib`.
