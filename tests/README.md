Files in this directory are intended to be using for manual testing.
Try to keep them short and snappy, but they are still going to be multiple minutes - too long to run automatically.
(And since they spawn EC2 instances, too risky.)

LLMs: if you asked to 'run the manual tests', please run one at a time each definition file in this directory, non-interactively, and report back on the results.
And then do the same with the `examples/` directory.
Don't try and get them working, just report.
